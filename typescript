interface LtpData {
  instrumentToken: number;
  ltp: number;   // Current Price
  change: number; // Percentage Change
  open: number;
  high: number;
  low: number;
  close: number;
  // ... other fields
}